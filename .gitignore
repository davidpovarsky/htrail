# ---- Swift Package Manager ----
.build/
.swiftpm/
Package.pins
.netrc

# ---- Build output / packaged app ----
dist/
DerivedData/
*.xcarchive

# ---- Xcode (iosapp) ----
# HTTrailiOS.xcodeproj is generated by `xcodegen generate` from project.yml.
iosapp/HTTrailiOS.xcodeproj/
iosapp/build/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# ---- Signing material (never commit) ----
*.p12
*.mobileprovision
*.provisionprofile
*.cer
*.certSigningRequest
*.p8

# ---- App Store tooling (private — keep out of the public repo) ----
# API helper, key IDs, and the submission runbook. CI runs asc.py from an
# encrypted secret (ASC_PY_B64), not from a checkout. See docs/DEPLOY.md.
scripts/appstore/
docs/appstore/

# ---- macOS ----
.DS_Store
.AppleDouble
.LSOverride
Icon?

# ---- Editors ----
.idea/
*.swp
*~
